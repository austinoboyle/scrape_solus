
class Job(object):
    def __init__(self, job_number, start, increment):
        self.letter = letter
        self.deep = deep
        self.callback = callback

    def finish(self, code):
        self.callback(code)
